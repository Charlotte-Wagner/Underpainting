# Python
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/

# Environment variables and secrets - never commit real values
.env
.env.*
!.env.example
.streamlit/secrets.toml
*.pem
*.key

# Editor / OS junk
.DS_Store
.vscode/
.idea/

# Build output
build/
dist/

# Test / coverage output
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# local coding-tool config, contains machine-specific paths
.claude/

# planning docs - not part of the public portfolio repo
BUILD-PLAN.md
build-plan-prompt.md
codex-brief-paint-lut.md
debug-log.md
paint-lut-spec.md
v2.md
teaching-rubric-draft.md
teaching-rubric-edited.md

# Per-session briefs and handoffs. By pattern rather than by filename, for the same
# reason the PDF rule below is by extension: every one of these was added by hand
# after the file already existed, which means each new session starts with an
# unignored planning doc in the repo root, one `git add .` from a public commit.
# S16's handoff was the eleventh in a row to need a line here. The glob covers the
# ten already listed and every one after it.
s[0-9]-brief.md
s[0-9]-handoff.md
s[0-9][0-9]-brief.md
s[0-9][0-9]-handoff.md

# Design and planning documents dropped into the project folder while working.
# Ignored by extension rather than by filename because the failure mode is a
# file nobody remembered to list: "Website flow 2.pdf" sat untracked in the repo
# root, one `git add .` away from a public commit, and it is the same category as
# BUILD-PLAN.md. No PDF has ever belonged in this repository's tracked files, so
# the broad pattern costs nothing and does not depend on anyone naming the next one.
*.pdf

# line-drawing prototypes and the photos they were measured on: working
# material for S12, not part of the app
prototypes/
test-photos/
